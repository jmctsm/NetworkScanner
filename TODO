add in v6 support
make pretty pictures
fix protocol scanners for all things scanned or as close as can
grab configs
parse configs

linux
    grab route table
    grab version
    
windows


arguments
    need one for putting in the domain name to pull for DNS

Figure out how to thread pinger and port_scanner

print everything out to a JSON file for reading later

exclude flag for IPs to not scan

